# ═══════════════════════════════════════════════════════════════════════════════
# SOVERSTACK CORE COMPUTE - Infrastructure VMs (Auto-generated)
# ═══════════════════════════════════════════════════════════════════════════════
#
# This file contains the mandatory infrastructure VMs for your Soverstack platform.
# DO NOT modify this file - your custom VMs should go in compute.yaml
#
# Documentation: https://docs.soverstack.io/infrastructure-requirements
#
# VM ID Ranges:
#   1-49:    Firewalls (VyOS)
#   50-69:   DNS Load Balancers (dnsdist)
#   70-99:   DNS Servers (PowerDNS)
#   100-149: Bastion/VPN (Headscale)
#   150-199: Secrets Management (OpenBao)
#   200-249: IAM/SSO (Keycloak)
#   250-279: Databases (PostgreSQL)
#   280-299: Cache (Redis)
#   300-319: Monitoring (Prometheus)
#   320-329: Alerting (Alertmanager)
#   330-349: Dashboards (Grafana)
#   350-369: Logging (Loki)
#   370-399: SIEM (Wazuh)
#   400-449: Load Balancers (HAProxy)
#   450-469: Tools (Soverstack)
#
# ═══════════════════════════════════════════════════════════════════════════════

# Instance type definitions for infrastructure VMs
instance_type_definitions:
  # ─────────────────────────────────────────────────────────────────────────────
  # NETWORKING
  # ─────────────────────────────────────────────────────────────────────────────
  - name: firewall-prod
    cpu: 4
    ram: 4
    disk: 20
    disk_type: local
    os_template: vyos-1.4

  - name: dns-lb-prod
    cpu: 2
    ram: 4
    disk: 20
    disk_type: distributed
    os_template: debian-12-cloudinit

  - name: dns-server-prod
    cpu: 2
    ram: 4
    disk: 40
    disk_type: distributed
    os_template: debian-12-cloudinit

  - name: lb-prod
    cpu: 2
    ram: 4
    disk: 20
    disk_type: distributed
    os_template: debian-12-cloudinit

  # ─────────────────────────────────────────────────────────────────────────────
  # ZERO-TRUST & SECURITY
  # ─────────────────────────────────────────────────────────────────────────────
  - name: bastion-prod
    cpu: 2
    ram: 4
    disk: 40
    disk_type: distributed
    os_template: debian-12-cloudinit

  - name: secrets-prod
    cpu: 2
    ram: 4
    disk: 40
    disk_type: distributed
    os_template: debian-12-cloudinit

  - name: iam-prod
    cpu: 4
    ram: 8
    disk: 40
    disk_type: distributed
    os_template: debian-12-cloudinit

  # ─────────────────────────────────────────────────────────────────────────────
  # DATA
  # ─────────────────────────────────────────────────────────────────────────────
  - name: database-prod
    cpu: 4
    ram: 8
    disk: 150
    disk_type: distributed
    os_template: debian-12-cloudinit

  - name: cache-prod
    cpu: 2
    ram: 8
    disk: 20
    disk_type: distributed
    os_template: debian-12-cloudinit

  # ─────────────────────────────────────────────────────────────────────────────
  # OBSERVABILITY
  # ─────────────────────────────────────────────────────────────────────────────
  - name: monitoring-prod
    cpu: 4
    ram: 16
    disk: 100
    disk_type: distributed
    os_template: debian-12-cloudinit

  - name: alerting-prod
    cpu: 2
    ram: 4
    disk: 20
    disk_type: distributed
    os_template: debian-12-cloudinit

  - name: dashboards-prod
    cpu: 2
    ram: 4
    disk: 20
    disk_type: distributed
    os_template: debian-12-cloudinit

  - name: logging-prod
    cpu: 4
    ram: 8
    disk: 100
    disk_type: distributed
    os_template: debian-12-cloudinit

  - name: siem-prod
    cpu: 4
    ram: 8
    disk: 100
    disk_type: distributed
    os_template: debian-12-cloudinit

  # ─────────────────────────────────────────────────────────────────────────────
  # TOOLS
  # ─────────────────────────────────────────────────────────────────────────────
  - name: soverstack-mgmt
    cpu: 1
    ram: 2
    disk: 40
    disk_type: distributed
    os_template: debian-12-cloudinit

# ═══════════════════════════════════════════════════════════════════════════════
# INFRASTRUCTURE VMs
# ═══════════════════════════════════════════════════════════════════════════════

virtual_machines:
  # ─────────────────────────────────────────────────────────────────────────────
  # FIREWALLS (VyOS) - HA pair with VRRP
  # ─────────────────────────────────────────────────────────────────────────────
  - name: vyos-01
    vm_id: 1
    host: pve-01
    role: firewall
    type_definition: firewall-prod

  - name: vyos-02
    vm_id: 2
    host: pve-02
    role: firewall
    type_definition: firewall-prod

  # ─────────────────────────────────────────────────────────────────────────────
  # LOAD BALANCERS (HAProxy) - HA pair with Keepalived
  # ─────────────────────────────────────────────────────────────────────────────
  - name: haproxy-01
    vm_id: 400
    host: pve-01
    role: load_balancer
    type_definition: lb-prod

  - name: haproxy-02
    vm_id: 401
    host: pve-02
    role: load_balancer
    type_definition: lb-prod

  # ─────────────────────────────────────────────────────────────────────────────
  # DNS (PowerDNS + dnsdist)
  # ─────────────────────────────────────────────────────────────────────────────
  - name: dnsdist-01
    vm_id: 50
    host: pve-01
    role: dns_lb
    type_definition: dns-lb-prod

  - name: dnsdist-02
    vm_id: 51
    host: pve-02
    role: dns_lb
    type_definition: dns-lb-prod

  - name: powerdns-01
    vm_id: 70
    host: pve-01
    role: dns_server
    type_definition: dns-server-prod

  - name: powerdns-02
    vm_id: 71
    host: pve-02
    role: dns_server
    type_definition: dns-server-prod

  # ─────────────────────────────────────────────────────────────────────────────
  # BASTION (Headscale) - VPN entry point
  # ─────────────────────────────────────────────────────────────────────────────
  - name: headscale-01
    vm_id: 100
    host: pve-01
    role: bastion
    type_definition: bastion-prod

  - name: headscale-02
    vm_id: 101
    host: pve-02
    role: bastion
    type_definition: bastion-prod

  # ─────────────────────────────────────────────────────────────────────────────
  # SECRETS (OpenBao) - Secrets management with Raft
  # ─────────────────────────────────────────────────────────────────────────────
  - name: openbao-01
    vm_id: 150
    host: pve-01
    role: secrets
    type_definition: secrets-prod

  - name: openbao-02
    vm_id: 151
    host: pve-02
    role: secrets
    type_definition: secrets-prod

  # ─────────────────────────────────────────────────────────────────────────────
  # IAM/SSO (Keycloak) - Identity provider
  # ─────────────────────────────────────────────────────────────────────────────
  - name: keycloak-01
    vm_id: 200
    host: pve-01
    role: iam_sso
    type_definition: iam-prod

  - name: keycloak-02
    vm_id: 201
    host: pve-02
    role: iam_sso
    type_definition: iam-prod

  # ─────────────────────────────────────────────────────────────────────────────
  # DATABASES (PostgreSQL) - HA with Patroni + etcd
  # ─────────────────────────────────────────────────────────────────────────────
  - name: postgres-01
    vm_id: 250
    host: pve-01
    role: database
    type_definition: database-prod

  - name: postgres-02
    vm_id: 251
    host: pve-02
    role: database
    type_definition: database-prod

  - name: postgres-03
    vm_id: 252
    host: pve-03
    role: database
    type_definition: database-prod

  # ─────────────────────────────────────────────────────────────────────────────
  # CACHE (Redis) - HA with Sentinel
  # ─────────────────────────────────────────────────────────────────────────────
  - name: redis-01
    vm_id: 280
    host: pve-01
    role: cache
    type_definition: cache-prod

  - name: redis-02
    vm_id: 281
    host: pve-02
    role: cache
    type_definition: cache-prod

  - name: redis-03
    vm_id: 282
    host: pve-03
    role: cache
    type_definition: cache-prod

  # ─────────────────────────────────────────────────────────────────────────────
  # MONITORING (Prometheus) - Dual scrape
  # ─────────────────────────────────────────────────────────────────────────────
  - name: prometheus-01
    vm_id: 300
    host: pve-01
    role: monitoring
    type_definition: monitoring-prod

  - name: prometheus-02
    vm_id: 301
    host: pve-02
    role: monitoring
    type_definition: monitoring-prod

  # ─────────────────────────────────────────────────────────────────────────────
  # ALERTING (Alertmanager) - Gossip cluster
  # ─────────────────────────────────────────────────────────────────────────────
  - name: alertmanager-01
    vm_id: 320
    host: pve-01
    role: alerting
    type_definition: alerting-prod

  - name: alertmanager-02
    vm_id: 321
    host: pve-02
    role: alerting
    type_definition: alerting-prod

  # ─────────────────────────────────────────────────────────────────────────────
  # DASHBOARDS (Grafana) - LB + shared DB
  # ─────────────────────────────────────────────────────────────────────────────
  - name: grafana-01
    vm_id: 330
    host: pve-01
    role: dashboards
    type_definition: dashboards-prod

  - name: grafana-02
    vm_id: 331
    host: pve-02
    role: dashboards
    type_definition: dashboards-prod

  # ─────────────────────────────────────────────────────────────────────────────
  # LOGGING (Loki) - Memberlist
  # ─────────────────────────────────────────────────────────────────────────────
  - name: loki-01
    vm_id: 350
    host: pve-01
    role: logging
    type_definition: logging-prod

  - name: loki-02
    vm_id: 351
    host: pve-02
    role: logging
    type_definition: logging-prod

  # ─────────────────────────────────────────────────────────────────────────────
  # SIEM (Wazuh)
  # ─────────────────────────────────────────────────────────────────────────────
  - name: wazuh-01
    vm_id: 370
    host: pve-01
    role: siem
    type_definition: siem-prod

  # ─────────────────────────────────────────────────────────────────────────────
  # SOVERSTACK MANAGEMENT
  # ─────────────────────────────────────────────────────────────────────────────
  - name: soverstack-01
    vm_id: 450
    host: pve-01
    role: management
    type_definition: soverstack-mgmt

linux_containers: []
